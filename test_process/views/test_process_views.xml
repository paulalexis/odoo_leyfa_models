<odoo>
    <!-- List View -->
    <record id="view_test_process_list" model="ir.ui.view">
        <field name="name">test.process.list</field>
        <field name="model">test.process</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-success="state == 'done'"/>
                <field name="obstacle_detected"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_test_process_form" model="ir.ui.view">
        <field name="name">test.process.form</field>
        <field name="model">test.process</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_next" type="object" string="Étape Suivante" class="btn-primary" invisible="state == 'done'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name"/></h1>
                    </div>

                    <!-- Full Flowchart Container -->
                    <div class="o_horizontal_separator mt-4 mb-3" style="font-weight: bold; color: #714B67;">
                        Flux de Travail du Mesurage
                    </div>
                    <div style="background: white; border: 1px solid #dee2e6; border-radius: 8px; padding: 15px;">
                        <field name="mermaid_graph" widget="mermaid"/>
                    </div>

                    <group>
                        <group string="Configuration">
                            <field name="obstacle_detected" widget="boolean_toggle"/>
                            <p class="text-muted" invisible="not obstacle_detected">
                                ℹ️ Le chemin passera par l'étape 'Maintenance'.
                            </p>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_test_process" model="ir.actions.act_window">
        <field name="name">Mesurage SNCF</field>
        <field name="res_model">test.process</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menus -->
    <menuitem id="menu_sncf_root" name="Mesurage SNCF" sequence="10"/>
    <menuitem id="menu_sncf_main" name="Processus" parent="menu_sncf_root" action="action_test_process"/>
</odoo>